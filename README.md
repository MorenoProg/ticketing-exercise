# Ticketing Exercise

## Run backend

```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
fastapi dev app.py
```

To inspect the API endpoints, open http://127.0.0.1:8000/docs